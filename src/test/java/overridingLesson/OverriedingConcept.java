package overridingLesson;

public class OverriedingConcept {
	
	/*
	 * Overriding / Dynamic binding / late binding / run time Polymorpism 
	 * Different classes (with inheritance applied) we have methods with same signature
	 * 
	 * Q1: Can we change the return type while performing overriding: Answer - No, When data types are primitive
	 * Yes, In case the return type are covariant & class name.
	 * 
	 * 
	 */

}
